# Tick 007-001: Fake Adapter Interface Only

Date: 2026-05-15

## Status

Complete.

## Summary

Added a private fake email adapter interface without enabling real sending.

Implemented:

- `backend/app/email_delivery/adapters.py`
- `backend/app/email_delivery/handoff.py`
- `backend/app/email_delivery/__init__.py`
- `backend/tests/test_email_adapter_handoff.py`

Updated:

- `backend/tests/test_boundaries.py`
- `BACKLOG.md`
- `docs/AGENT_BRIEF.md`
- `reviews/tick-007-001-fake-adapter-interface.md`

## Safety Boundary

The fake adapter is not exposed through FastAPI and performs no network I/O. `EmailHandoffService` refuses to invoke even the fake adapter unless the backend has a `reserved_for_send` gate result and a matching active/reserved `SendReservation`.

No Gmail, SMTP, external provider, public send endpoint, OpenAI dependency, outreach record creation, or real send path was added.

A review pass found and fixed two gaps before closure:

- The handoff now rejects non-fake adapter providers during this phase.
- Public API tests now check broader send/handoff endpoint fragments, not only exact `/send`.

## Verification

Ran:

```powershell
uv run --python 3.12 --extra test pytest backend/tests/test_email_adapter_handoff.py backend/tests/test_import_api.py::test_no_send_endpoint_exists backend/tests/test_boundaries.py -q
```

Result: `14 passed`.

Ran full suite:

```powershell
uv run --python 3.12 --extra test pytest -q --basetemp=.tmp\pytest-tick-007-001
```

Result: `196 passed, 1 skipped`.

## Notes

`reserve_for_send` remains future work. This tick tests the handoff contract by constructing reserved gate and reservation records directly.

RPG was not lifted or used for this tick.
