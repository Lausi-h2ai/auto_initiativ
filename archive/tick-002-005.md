# AM-002-005: Add Phase 2 Seed Fixtures

Status: `done`
Date: 2026-05-14

## Objective

Create fake, deterministic local development fixtures for Phase 2 profile, policy, company, contact, draft, and send-intent domain records.

## Changes

- Added `backend.app.seed.fixtures` with deterministic Phase 2 seed payloads.
- Added `add_phase2_seed_records(session)` to populate safe local seed rows through the existing SQLModel domain models.
- Added seed fixture tests covering deterministic payload generation, fake/safe data constraints, persistence links, normalization fields, and boundary behavior.
- Confirmed the seed helper does not create gate results, send reservations, outreach records, email adapters, Gmail usage, or send paths.

## Verification

- `uv run --python 3.12 --extra test pytest backend/tests/test_seed_fixtures.py -q`
- `uv run --python 3.12 --extra test pytest -q`

## Audit

No gate execution, send reservation creation, outreach record creation, email adapter, Gmail access, OpenAI API dependency, send endpoint, or autonomous sending behavior was added.

## Next Tick

`AM-002-006: Import Valid Outputs Into Domain Tables`
