# Tick AM-002-002 Archive

## Objective

Select and document the Phase 2 migration strategy before normalized domain model work.

User decisions for this tick:

- Use SQLModel because Phase 1 already uses SQLModel.
- Use Alembic now.
- Keep SQLite support only for fast unit tests where possible.
- Use Postgres for constraint and migration integration tests.
- Do not support SQLite as an equal production backend.
- Avoid destructive migrations.

## Staged Passes

### PLAN

Scoped the tick to migration strategy and minimal migration infrastructure only.

Out of scope:

- Phase 2 domain models.
- Dedupe constraints.
- Phase 3 gate execution.
- Email adapters or Gmail sending.
- OpenAI API usage.
- Frontend work.

### RESEARCH/INSPECT

Consulted current Alembic documentation for `target_metadata`, autogenerate, and `env.py` database URL configuration.

Inspected:

- `backend/app/core/config.py`
- `backend/app/db/session.py`
- `backend/app/db/models.py`
- existing tests and Phase 2 planning docs

### DECIDE

Created `adr/0001-migration-strategy.md`.

Decision:

- Alembic is the migration tool.
- SQLModel remains the ORM/model layer.
- Alembic reads `DATABASE_URL` through backend settings.
- SQLite remains a fast-test convenience only.
- Postgres is required for migration/constraint integration tests.

### MINIMAL IMPLEMENT

Added:

- `alembic.ini`
- `backend/migrations/env.py`
- `backend/migrations/script.py.mako`
- `backend/migrations/README.md`
- `backend/migrations/versions/20260513_0001_phase1_operational_tables.py`
- `backend/tests/test_migrations.py`

Updated:

- `pyproject.toml`
- `uv.lock`
- `BACKLOG.md`

### TEST

Ran targeted migration tests:

```text
uv run --python 3.12 --extra test pytest backend/tests/test_migrations.py -q
3 passed
```

Ran full test suite:

```text
uv run --python 3.12 --extra test pytest
44 passed
```

Ran Alembic CLI smoke check against a temporary SQLite database:

```text
uv run --python 3.12 alembic upgrade head
uv run --python 3.12 alembic check
No new upgrade operations detected.
```

### REVIEW/FIX

Fixed one test issue where Alembic console output was not captured reliably by pytest. The test now reads `alembic_version` directly.

Disabled the destructive initial migration downgrade and added a regression test proving downgrade refuses to drop Phase 1 operational tables.

Boundary search found no Gmail/OpenAI/adapter implementation. Matches were limited to boundary tests asserting those are absent.

### AUDIT

Verdict: complete.

Tick AM-002-002 is complete and safe to stop. The next recommended tick is `AM-002-003`.

## Remaining Risks

- Postgres-backed migration/constraint integration tests are not implemented yet because no Phase 2 constraints exist in this tick.
- Existing Phase 1 local databases created with `create_all()` should be stamped after schema confirmation, not destructively migrated.
- The Phase 3 gate status schema/docs mismatch remains deferred.

## Commit

Not committed in this tick.
